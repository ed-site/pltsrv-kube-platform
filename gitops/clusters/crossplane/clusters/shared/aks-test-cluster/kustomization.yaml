namespace: aks-test-cluster

resources:
  - ../base
  - namespace.yaml

patches:
- path: cluster-claim.yaml 
  target:
    group: kubernetes.mckesson.com
    version: v1alpha1
    kind: AksClusterClaim
    name: aks-test-cluster
- patch: |-
    - op: replace
      path: /metadata/name
      value: aks-test-cluster  
  target:
    kind: AksClusterClaim 